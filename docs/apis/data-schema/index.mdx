---
title: Data schema
sidebar_position: 1
sidebar_label: Data schema
slug: ./
hide_table_of_contents: true
---

import Link from '@docusaurus/Link';
import categories from './categories.ts';

export default function PrettyCategories() {
  return (
    <div className="container mx-auto px-4 py-8">
      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-16">
        {categories.map((category, index) => (
          <div key={index} className="bg-[#004B48] rounded-lg shadow-md hover:shadow-lg transition-shadow duration-300 flex flex-col">
            <div className="p-6 pb-0 border-b border-[#006B68]">
              <h2 className="text-2xl font-bold text-white">{category.name}</h2>
            </div>
            <div className="p-6 pt-0 flex-grow">
              <div className="space-y-4">
                {category.entities.map((entity, entityIndex) => (
                  <div key={entityIndex}>
                    {/*<a href={entity.link} className="text-[#00D1C7] hover:underline">*/}
                      <h3 className="text-lg font-semibold mb-1">{entity.name}</h3>
                    {/*</a>*/}
                    <p className="text-sm text-[#A0F0ED]">{entity.description}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  )
}


