---
sidebar_label: elfsquad/third-party-visualization
slug: elfsquad-third-party-visualization-library
---

import visualizationFrameMethods from './methods/visualizationFrame';
import eventSenderMethods from './methods/eventSender';
import { MethodDocument } from '../../components/MethodDocument';
import { MethodCategory } from '../../components/MethodCategory';
import Link from '@docusaurus/Link';


export const Content = ({ children }) => {
  const visualizationFrameName = 'VisualizationFrame';
  const visualizationFrameDescription = 'The VisualizationFrame class is the main entry point for the elfsquad/third-party-visualization library. It provides methods to embed & communicate with custom visualization.';

  return <div class="space-y-32">
    <div class="flex justify-between items-center">
      {children}
      <Link to="https://github.com/elfsquad/third-party-visualization">
        <button class="bg-gray-700 text-white px-4 py-2 rounded-md flex items-center gap-2 mt-[-18px]">
          <img src="/img/github-mark-white.svg" style={{height:24, padding: 0}} />
          View on GitHub
        </button>
      </Link>
    </div>
    <MethodCategory name={visualizationFrameName} description={visualizationFrameDescription} />
    {visualizationFrameMethods.map(m => <MethodDocument {...m} />)}

    <MethodCategory name="EventSender" description="The EventSender class provides methods to send events to the parent window." />
    {eventSenderMethods.map(m => <MethodDocument {...m} />)}
  </div>
}

<Content>
  # @elfsquad/third-party-visualization
</Content>

<div class="hidden">
  ## VisualizationFrame
  ### constructor
  ### getNativeElement
  ### onTriggerConfigurationUpdate
  ### onUpdateRequirement
  ### onUpdateRequirements
  ### onUpdateImageValue
  ### onUpdateTextValue
  ### onUpdateLinkedConfigurationCardinality
  ### onDragStarted
  ### onRemoveLinkedConfiguration
  ### sendConfigurationUpdated
  ### sendStepChanged

  ## EventSender
  ### constructor
  ### sendUpdateRequirement
  ### sendDragStarted
  ### sendUpdateRequirements
  ### sendUpdateImageValue
  ### sendUpdateTextValue
  ### sendUpdateLinkedConfigurationCardinality
  ### sendRemoveLinkedConfiguration
</div>

